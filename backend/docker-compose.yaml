version: "3.9"
services:

  postgres-back:
    image: postgres:15.3-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rats
      POSTGRES_DB: rats
    healthcheck:
      test: ["CMD", "sh", "-c", "pg_isready -U $$POSTGRES_USER -h $$(hostname -i)"]
      interval: 5s
      timeout: 5s
      retries: 5
    user: postgres

  license-manager:
    build:
      context: .
      dockerfile: ./Dockerfile
    depends_on:
      postgres-back:
        condition: service_healthy
    volumes:
      - ./lm_backend:/app/lm_backend
    environment:
      #DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:123@postgres-back:5432/postgres}
      DATABASE_URL: postgresql+asyncpg://postgres:rats@postgres-back:5432/rats
      ARMASEC_DOMAIN: ${ARMASEC_DOMAIN}
      ARMASEC_AUDIENCE: ${ARMASEC_AUDIENCE}
      ARMASEC_DEBUG: ${ARMASEC_DEBUG}
      LOG_LEVEL: ${LOG_LEVEL}
    ports:
      - "7000:8000"
    command: uvicorn lm_backend.main:app --reload --workers 1 --host 0.0.0.0 --port 8000

  unittest:
    build:
      context: .
      dockerfile: ./Dockerfile-ci
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:rats@pgsql:5432/ratsdb
      ARMASEC_DOMAIN: armasec.dev
      ARMASEC_AUDIENCE: https://armasec.dev
      LOG_LEVEL: DEBUG
    command: pytest -s
    depends_on:
      pgsql:
        condition: service_healthy
      migration:
        condition: service_completed_successfully
    volumes:
      - ./tests:/app/tests

  migration:
    build:
      context: .
      dockerfile: ./Dockerfile-ci
    environment:
      DATABASE_URL: postgresql://postgres:rats@pgsql:5432/ratsdb
      ARMASEC_DOMAIN: armasec.dev
      ARMASEC_AUDIENCE: https://armasec.dev
      LOG_LEVEL: DEBUG
    command: "alembic -c alembic/alembic.ini upgrade head"
    depends_on:
      pgsql:
        condition: service_healthy

  pgsql:
    image: postgres:15.3-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: rats
      POSTGRES_DB: ratsdb
    ports: ["5432:5432"]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "sh", "-c", "pg_isready -U $$POSTGRES_USER -h $$(hostname -i)"]
      interval: 10s
      timeout: 5s
      retries: 5
