#!/bin/bash

set -e

snap_mode=$(cat $SNAP_COMMON/snap_mode.txt)

if ! [[ $snap_mode == "server" ]]; then
    exit 0
fi
exec "$SNAP/bin/license-server" -l "$SNAP_COMMON/license_manager/log/server.log" -c "$SNAP_COMMON/license_manager/config/server.yaml"
